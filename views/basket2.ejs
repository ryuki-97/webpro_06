<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>バスケ選手名簿</title>
</head>
<body>
  <h2>バスケ選手名簿</h2>
  <table>
    <tr><th>ID</th><th>チーム名</th><th>選手名</th></tr>
      <% let number = 0 %>
      <% for( let row of data ) { %>
        <tr><td><%= row.id %></td>
        <td><%= row.team %></td>
        <td><a href="/basket2/<%= number %>"><%= row.name %></a></td></tr>
        <% number += 1 %>
      <% } %>
  </table>
  <a href="/basket2/create">追加</a>
</body>
</html>